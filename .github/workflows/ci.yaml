# Trigger on push
on: [push, pull_request]

jobs:
  test:
    runs-on: macOS-10.14
    name: "Test layer"
    steps:
      - name: Create layer and run single example lambda
      - run: make test_layer
